<section class="tab general {{tab.cssClass}} scrollable" data-group="primary" data-tab="generalAttr">
	<div class="content-item">

		<div class="general-attributes-section op-shadow">
			<div class="section-title">Atributos Gerais</div>

			<div class="resource grid grid-2col">
				<label class="resource-label">{{localize "op.category"}}</label>
				<div class="categoryCheck">
					{{radioBoxes "system.category" categories checked=system.category}}
				</div>
			</div>
		</div>

		<div class="general-attributes-section op-shadow" style="margin-top: 12px;">
			<div class="section-title">{{localize "op.weaponModSection"}}</div>
			<div class="grid" style="grid-template-columns: 120px 1fr; gap: 10px; align-items: center;">
				<label class="resource-label">{{localize "op.modifiesWeapons"}}</label>
				<input type="checkbox" name="system.modifiesWeapons" {{checked system.modifiesWeapons}} />

				<label class="resource-label">{{localize "op.attackBonus"}}</label>
				<input type="number" name="system.weaponMod.attackBonus" value="{{system.weaponMod.attackBonus}}" />

				<label class="resource-label">{{localize "op.damageBonusFormula"}}</label>
				<input type="text" name="system.weaponMod.damageBonus" value="{{system.weaponMod.damageBonus}}" placeholder="1d6" />
			</div>
		</div>

	</div>
</section>